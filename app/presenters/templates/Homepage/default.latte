{block content}
    <h1 n:block='title'>Můj blog</h1>

    {foreach $posts as $post}
        <div class='post'>
            <h2><a href='{link Post:show $post->id}'>{$post->title}</a></h2>
            <p>{$post->content}</p>
            <div class='date'>{$post->created_at|date:'j. F Y, G:i'}</div>
            <hr>
        </div>
    {/foreach}